.section
  .contents
    %h2 
      コーヒーの楽しさを知る
    .columns
      %h2 投稿一覧
      タグリスト
      - @tag_list.each do |list|
        = link_to list.tag_name, search_blogs_path(blog_tag_id: list.id)
        = "(#{list.blogs.count})"
      - @blogs.each do |blog|
        = link_to blog_path(blog.id) do
          %section
            .blogList
              .blogList__image
                = image_tag blog.image.url size: '220x150' 
              .blogList__body
                = blog.title
              .blogList__author
                = blog.user.name